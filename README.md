# auto_test
Use dynamic symbolic execution with Z3 to auto generate unit test cases that reach full code coverage

# Usage
```
pip install -r requirements.txt && python -m PythonSE.main && EXPECTTEST_ACCEPT=1 pytest PythonSE/AutoGeneratedUnitTests
```
